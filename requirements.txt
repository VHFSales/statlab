# StatLab dependencies.
#
# The statistical CORE (statlab/statistics/) requires NO third-party packages —
# it is pure Python standard library, by design (transparency, auditability,
# offline testability). The packages below are needed only for the OUTER layers
# (import, plotting, Excel/PDF export, UI) and as optional validation oracles.
#
# Install for the full application:
#     pip install -r requirements.txt

# --- Data import / wrangling (data/importer.py) ---
pandas>=1.3
openpyxl>=3.0        # .xlsx read/write (Excel export)

# --- Plotting (plots/scientific_plots.py) ---
matplotlib>=3.4      # scientific plots + PDF via PdfPages

# --- User interface (app/ui/streamlit_app.py) ---
streamlit>=1.20

# --- OPTIONAL: validation oracles (tests/test_oracle.py) ---
# Uncomment to cross-validate the pure-Python core against reference libraries.
# scipy>=1.7
# statsmodels>=0.13
# numpy>=1.21

# --- OPTIONAL: legacy .xls reading (best-effort) ---
# xlrd>=2.0
